---
'@gerege-systems/ui': patch
---

Form control and outline button borders now match the card border: `--border-input` equals `--border` (`hsl(214 32% 91%)` light / `hsl(217 33% 17%)` dark). `prefers-contrast: more` still restores 3:1 values.
